[
 {
  "source": "rules/objects/conditions",
  "target": "rules/objects/durability",
  "class": "mechanism",
  "note": "The three conditions are VIT thresholds defined by Object Durability (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/objects/armor-degradation",
  "target": "rules/objects/conditions",
  "class": "mechanism",
  "note": "Uses Damaged/Broken as defined terms (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/objects/shield-degradation",
  "target": "rules/objects/conditions",
  "class": "mechanism",
  "note": "Uses Damaged/Broken as defined terms (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/objects/shield-degradation",
  "target": "rules/basics/supply-rolls",
  "class": "mechanism",
  "note": "Shield wear runs on supply dice (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/vehicles/boarding-and-bailing",
  "target": "rules/movement/speed-tiers-chart",
  "class": "mechanism",
  "note": "Targets are 6 + speed modifier from the chart (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/vehicles/collision-damage",
  "target": "rules/movement/speed-tiers-chart",
  "class": "mechanism",
  "note": "Formula uses the chart's speed modifier (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/vehicles/impact-and-overrun",
  "target": "rules/vehicles/collision-damage",
  "class": "mechanism",
  "note": "Failure outcomes resolve via collision damage (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/vehicles/mounts",
  "target": "rules/vehicles/collision-damage",
  "class": "mechanism",
  "note": "Sudden Stops deals collision damage (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/health/applying-damage",
  "target": "rules/stats/defense",
  "class": "mechanism",
  "note": "The loop is defined in terms of DEF (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/health/applying-damage",
  "target": "rules/stats/vitality",
  "class": "mechanism",
  "note": "The loop is defined in terms of VIT (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/combat/damage-dice",
  "target": "core-rules/size",
  "class": "mechanism",
  "note": "Dice determined by attacker/defender size comparison (implicit — no link in text; ruled reference)"
 },
 {
  "source": "gear/sci-fi/kits/kit-supplies",
  "target": "rules/basics/supply-rolls",
  "class": "mechanism",
  "note": "Kit supplies run on supply dice (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/conditions/stressed",
  "target": "rules/stats/stress-threshold",
  "class": "mechanism",
  "note": "Trigger defined by Stress Threshold (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/conditions/dying",
  "target": "rules/conditions/unconscious",
  "class": "mechanism",
  "note": "Dying character 'immediately falls unconscious (as the condition)' (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/sci-fi/energy-shields",
  "target": "rules/stats/defense",
  "class": "mechanism",
  "note": "Temporary DEF pool concept extends DEF (implicit — no link in text; ruled reference)"
 },
 {
  "source": "rules/stats/luck",
  "target": "rules/conditions/stressed",
  "class": "mechanism",
  "note": "Stressed disables Luck (implicit — no link in text; ruled reference)"
 }
]
