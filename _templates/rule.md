---
title: "Cover"
id: rules/combat/cover
category: [core]
type: rule
tags: [combat]
---

Any solid object that an attack cannot penetrate can be used as cover.

**Partial Cover** — At least half of the defender is behind a solid object.

<!--
NO HEADING. This is a SECTION BLOCK: the host page supplies the heading, so the
file starts straight into prose. blocks.json records `owns_heading: false`, and
anything re-assembling the block elsewhere ({{< blockset >}}, the PDF builder)
supplies a heading from `title`. That makes `title` load-bearing — it must read
as the name a reader sees, not a tidied-up version of the id.

If the block should carry its own heading instead, use rule-standalone.md.

No `summary` here because no catalog lists it. Add one if that changes.

Delete this comment.
-->
